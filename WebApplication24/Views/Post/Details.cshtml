@*@model WebApplication24.Models.JobPost

    @{
        ViewBag.Title = Model.Department + "Recruitment 2025";

    }


    <!DOCTYPE html>
    <html lang="en">
    <head>
        <link href="~/css/Details.css" rel="stylesheet" />

        <title>@Model.Department Recruitment 2025</title>

    </head>
    <body>

        @{
            var postNames = Model.Posts?.Select(p => p.PostName).ToList() ?? new List<string>();
            var postSummary = "";

            if (postNames.Count == 0)
            {
                postSummary = "N/A";
            }
            else if (postNames.Count == 1)
            {
                postSummary = postNames[0];
            }
            else if (postNames.Count == 2)
            {
                postSummary = string.Join(", ", postNames);
            }
            else
            {
                postSummary = string.Join(", ", postNames.Take(2)) + " and More";
            }
            var FullpostName = Model.Posts?.Select(p => p.PostName).ToList() ?? new List<string>();
            var FullpostSummary = FullpostName.Count > 0 ? string.Join(", ", FullpostName) : "N/A";
            var qualifications = Model.Posts?.Select(p => p.Qualification).ToList() ?? new List<string>();
            var qualificationSummary = qualifications.Count > 0 ? string.Join(", ", qualifications) : "N/A";
        }
        @{

        }


        <div class="container">
            <h2>
                <span class="highlight">@Model.Department Recruitment 2025</span> - Apply Online for @Model.NO_Post @postSummary Posts
            </h2>
            <p><strong>Updated:</strong> <span class="highlight">05 June 2025</span></p>
            <p><strong>Post Published By:</strong> <span class="highlight">J Nandhini</span></p>

            <h4>Job Overview</h4>
            <p>
                The @Model.Department has announced the recruitment for the  @Model.NO_Post post of @postSummary .
                This opportunity is open to candidates who possess qualifications such as @qualificationSummary Can Apply Offline.
                A total of @Model.NO_Post post has been declared, and eligible candidates can apply online between
                @Model.AdvertisementDate.ToString("dd-MMMM-yyyy") and @Model.LastDate.ToString("dd-MMMM-yyyy").
            </p>

            <h5>Vacancy Details</h5>
            <table class="responsive-table">
                <thead>
                    <tr>
                        <th>Field</th>
                        <th>Details</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td data-label="Field">Post Name</td>
                        <td data-label="Details">@FullpostSummary .</td>
                    </tr>
                    <tr>
                        <td data-label="Field">Advt No.</td>
                        <td data-label="Details"><span class="highlight">HRDD/RECT/02/2025-26</span></td>
                    </tr>
                    <tr>
                        <td data-label="Field">Total Vacancies</td>
                        <td data-label="Details">@Model.NO_Post</td>
                    </tr>
                    <tr>
                        <td data-label="Field">Qualification xxxxxxxxxxxx</td>
                        <td data-label="Details"><span class="highlight">CA / CS / ICSI</span> with ACS membership</td>
                    </tr>
                    <tr>
                        <td data-label="Field">Minimum Age</td>
                        <td data-label="Details"><span class="highlight">35 Years</span></td>
                    </tr>
                    <tr>
                        <td data-label="Field">Maximum Age</td>
                        <td data-label="Details"><span class="highlight">50 Years</span></td>
                    </tr>
                    <tr>
                        <td data-label="Field">Application Start Date</td>
                        <td data-label="Details">@Model.AdvertisementDate.ToString("dd-MMMM-yyyy")</td>
                    </tr>
                    <tr>
                        <td data-label="Field">Last Date to Apply</td>
                        <td data-label="Details">@Model.LastDate.ToString("dd-MMMM-yyyy")</td>
                    </tr>
                    <tr>
                        <td data-label="Field">Fee for SC/ST/PwBD</td>
                        <td data-label="Details"><span class="highlight">₹175</span> (including GST)</td>
                    </tr>
                    <tr>
                        <td data-label="Field">Fee for General/OBC/EWS</td>
                        <td data-label="Details"><span class="highlight">₹1000</span> (including GST)</td>
                    </tr>
                </tbody>
            </table>

            <h5>Eligibility Criteria</h5>

            <table class="responsive-table">
                <tbody>
                    @foreach (var any in Model.Posts) // Qualification with post Name
                    {
                        <tr>
                            <td data-label="Post">@any.PostName</td>
                            <td data-label="Qualification">@any.Qualification</td>
                        </tr>
                    }
                </tbody>
            </table>



            <h5>Age Criteria</h5>
            <table class="responsive-table">
                <tbody>
                    <tr>
                        <td data-label="Criteria">Minimum Age</td>
                        <td data-label="Details"><span class="highlight">18 years</span></td>
                    </tr>
                    <tr>
                        <td data-label="Criteria">Maximum Age</td>
                        <td data-label="Details"><span class="highlight">22 years</span></td>
                    </tr>
                    <tr>
                        <td data-label="Criteria">Relaxation</td>
                        <td data-label="Details"><span class="highlight">As per government norms</span></td>
                    </tr>
                </tbody>
            </table>

            <h5>Application Fees</h5>
            <table class="responsive-table">
                <tbody>
                    <tr>
                        <td data-label="Category">General / OBC / EWS</td>
                        <td data-label="Fee"><span class="highlight">₹300</span></td>
                    </tr>
                    <tr>
                        <td data-label="Category">SC / ST</td>
                        <td data-label="Fee"><span class="highlight">No Fee</span></td>
                    </tr>
                </tbody>
            </table>

            <h5>Salary</h5>
            <table class="responsive-table">
                <tbody>
                    <tr>
                        <td data-label="Post">Navik (GD/DB)</td>
                        <td data-label="Pay"><span class="highlight">₹21,700/- Level-3 + DA</span></td>
                    </tr>
                    <tr>
                        <td data-label="Post">Yantrik</td>
                        <td data-label="Pay"><span class="highlight">₹29,200/- Level-5 + ₹6,200/- Yantrik Pay + DA</span></td>
                    </tr>
                </tbody>
            </table>

            <h5>Important Dates</h5>
            <table class="responsive-table">
                <tbody>
                    <tr>
                        <td data-label="Event">Start Date to Apply Online</td>
                        <td data-label="Date">@Model.AdvertisementDate.ToString("dd-MMMM-yyyy")</td>
                    </tr>
                    <tr>
                        <td data-label="Event">Last Date to Apply Online</td>
                        <td data-label="Date">@Model.LastDate.ToString("dd-MMMM-yyyy")</td>
                    </tr>
                </tbody>
            </table>

            <h5>Important Links</h5>
            <table class="responsive-table">
                <tbody>
                    @foreach (var may in Model.Sections)
                    {
                        <tr>
                            <td data-label="Label"> @may.Label</td>
                            <td data-label="Link"><a href="@may.Text" target="_blank">@may.Text</a></td>
                        </tr>
                    }
                </tbody>

            </table>


            <p class="footer-note">© <span class="highlight">2025</span> Indian Overseas Bank. All Rights Reserved.</p>
        </div>
    </body>
    </html>*@
@model WebApplication24.Models.JobPost

@{
    ViewBag.Title = Model.Department + " Recruitment 2025";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <link href="~/css/Details.css" rel="stylesheet" />
    <meta charset="utf-8" />
    <title>@Model.Department Recruitment 2025</title>
</head>
<body>

    @{
        var postNames = Model.Posts?.Select(p => p.PostName).ToList() ?? new List<string>();
        var postSummary = "";

        if (postNames.Count == 0)
        {
            postSummary = "N/A";
        }
        else if (postNames.Count == 1)
        {
            postSummary = postNames[0];
        }
        else if (postNames.Count == 2)
        {
            postSummary = string.Join(", ", postNames);
        }
        else
        {
            postSummary = string.Join(", ", postNames.Take(2)) + " and More";
        }

        var FullpostName = Model.Posts?.Select(p => p.PostName).ToList() ?? new List<string>();
        var FullpostSummary = FullpostName.Count > 0 ? string.Join(", ", FullpostName) : "N/A";
        var qualifications = Model.Posts?.Select(p => p.Qualification).ToList() ?? new List<string>();
        var qualificationSummary = qualifications.Count > 0 ? string.Join(", ", qualifications) : "N/A";
    }

    <div class="container">
        <h2>
            <span class="highlight">@Model.Department Recruitment 2025</span> - Apply Online for @Model.NO_Post @postSummary Posts
        </h2>
        <p><strong>Updated:</strong> <span class="highlight">05 June 2025</span></p>
        <p><strong>Post Published By:</strong> <span class="highlight">J Nandhini</span></p>

        <h4>Job Overview</h4>
        <p>
            The @Model.Department has officially released the recruitment notification for @Model.NO_Post posts including @postSummary.
            This is a golden opportunity for candidates interested in joining the prestigious @Model.Department as part of the government workforce.
            Aspirants with educational qualifications are eligible to apply offline for the available positions.
            Interested applicants are encouraged to apply between @Model.AdvertisementDate.ToString("dd-MMMM-yyyy") and @Model.LastDate.ToString("dd-MMMM-yyyy") as per the official schedule.
        </p>

        <h5>Vacancy Details</h5>
        <p>The following section provides detailed information about the vacancies released by @Model.Department. This includes the list of available post names, the total number of posts, required qualifications, and other related recruitment information to help candidates understand the job profile before applying.</p>
        <table class="responsive-table">
            <thead>
                <tr>
                    <th>Field</th>
                    <th>Details</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td data-label="Field">Post Name</td>
                    <td data-label="Details">@FullpostSummary.</td>
                </tr>
                <tr>
                    <td data-label="Field">Advt No.</td>
                    <td data-label="Details"><span class="highlight">HRDD/RECT/02/2025-26</span></td>
                </tr>
                <tr>
                    <td data-label="Field">Total Vacancies</td>
                    <td data-label="Details">@Model.NO_Post</td>
                </tr>
                @*<tr>
                    <td data-label="Field">Qualification</td>
                    <td data-label="Details">@qualificationSummary</td>
                </tr>*@
                @*<tr>
                    <td data-label="Field">Minimum Age</td>
                    <td data-label="Details"><span class="highlight">35 Years</span></td>
                </tr>
                <tr>
                    <td data-label="Field">Maximum Age</td>
                    <td data-label="Details"><span class="highlight">50 Years</span></td>
                </tr>*@
                <tr>
                    <td data-label="Field">Application Start Date</td>
                    <td data-label="Details">@Model.AdvertisementDate.ToString("dd-MMMM-yyyy")</td>
                </tr>
                <tr>
                    <td data-label="Field">Last Date to Apply</td>
                    <td data-label="Details">@Model.LastDate.ToString("dd-MMMM-yyyy")</td>
                </tr>
                @*<tr>
                    <td data-label="Field">Fee for SC/ST/PwBD</td>
                    <td data-label="Details"><span class="highlight">₹175</span> (including GST)</td>
                </tr>
                <tr>
                    <td data-label="Field">Fee for General/OBC/EWS</td>
                    <td data-label="Details"><span class="highlight">₹1000</span> (including GST)</td>
                </tr>*@
            </tbody>
        </table>

        <h5>Eligibility Criteria</h5>
        <p>To apply for @Model.Department Recruitment 2025, candidates must meet the minimum educational qualifications as specified below. This section outlines post-wise eligibility criteria including academic backgrounds, certifications, and equivalent qualifications accepted by @Model.Department for the recruitment process.</p>
        <table class="responsive-table">
            <tbody>
                @foreach (var any in Model.Posts)
                {
                    <tr>
                        <td data-label="Post">@any.PostName</td>
                        <td data-label="Qualification">@any.Qualification</td>
                    </tr>
                }
            </tbody>
        </table>

        <h5>Age Criteria</h5>
        <p>@Model.Department has set specific age limits for different categories of candidates applying for various posts. The table below provides a breakdown of the minimum and maximum age requirements, along with relaxation rules as per government norms applicable for SC/ST/OBC/PwBD candidates.</p>
        <table class="responsive-table">
            <tbody>
                <tr>
                    <td data-label="Criteria">Minimum Age</td>
                    <td data-label="Details"><span class="highlight">18 years</span></td>
                </tr>
                <tr>
                    <td data-label="Criteria">Maximum Age</td>
                    <td data-label="Details"><span class="highlight">22 years</span></td>
                </tr>
                <tr>
                    <td data-label="Criteria">Relaxation</td>
                    <td data-label="Details"><span class="highlight">As per government norms</span></td>
                </tr>
            </tbody>
        </table>

        <h5>Application Fees</h5>
        <p>Candidates applying for the @Model.Department recruitment must pay a non-refundable application fee as per their category. The table below shows the category-wise application charges including GST. Ensure timely payment to avoid rejection of your application.</p>
        <table class="responsive-table">
            <tbody>
                @foreach (var item in Model.FEELIST)
                {
                    if (item.General_OBC_EWS != null)
                    {
                        <tr>
                            <td data-label="Category">General / OBC / EWS</td>
                            <td data-label="Fee">@item.General_OBC_EWS</td>
                        </tr>
                    }

                    if (item.SC_ST_PwD_Ex_Serviceman != null)
                    {
                        <tr>
                            <td data-label="Category">SC / ST</td>
                            <td data-label="Fee">@item.SC_ST_PwD_Ex_Serviceman</td>
                        </tr>
                    }

                    if (item.For_Girls != null)
                    {
                        <tr>
                            <td data-label="Category">For Girls</td>
                            <td data-label="Fee">@item.For_Girls</td>
                        </tr>
                    }
                }
            </tbody>

        </table>

        <h5>Salary</h5>
        <p>The @Model.Department offers a competitive pay scale to its employees. Below are the details of salary structures for various posts including basic pay, grade pay, and other applicable allowances as per the 7th Pay Commission rules.</p>
        <table class="responsive-table">
            <tbody>
                @foreach (var any in Model.Posts)
                {
                    <tr>
                        <td data-label="Post">@any.PostName</td>
                        <td data-label="Qualification">@any.Salary</td>
                    </tr>
                }
            </tbody>
        </table>

        <h5>Important Dates</h5>
        <p>Applicants should take note of all the important dates related to @Model.Department Recruitment 2025. From the application start date to the closing date, staying informed about deadlines is essential for successful registration.</p>
        <table class="responsive-table">
            <tbody>
                <tr>
                    <td data-label="Event">Start Date to Apply Online</td>
                    <td data-label="Date">@Model.AdvertisementDate.ToString("dd-MMMM-yyyy")</td>
                </tr>
                <tr>
                    <td data-label="Event">Last Date to Apply Online</td>
                    <td data-label="Date">@Model.LastDate.ToString("dd-MMMM-yyyy")</td>
                </tr>
            </tbody>
        </table>

        <h5>Important Links</h5>
        <p>All official links provided by @Model.Department for applying online, downloading notifications, and accessing application forms are listed below. Click the relevant link to proceed with your application or read the official document.</p>
        <table class="responsive-table">
            <tbody>
                @foreach (var may in Model.Sections)
                {
                    <tr>
                        <td data-label="Label">@may.Label</td>
                        <td data-label="Link"><a href="@may.Text" target="_blank">@may.Text</a></td>
                    </tr>
                }
            </tbody>
        </table>

        <p class="footer-note">© <span class="highlight">2025</span> Indian Overseas Bank. All Rights Reserved.</p>
    </div>

</body>
</html>
